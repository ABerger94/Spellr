services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: spellr
      POSTGRES_PASSWORD: spellr
      POSTGRES_DB: spellr
    ports:
      - '5432:5432'
    volumes:
      - spellr_postgres_data:/var/lib/postgresql/data

volumes:
  spellr_postgres_data:
